define(["jquery","slick"],function($){"strict";return{createSlider:function(id,config){if(config){$(id).not('.slick-initialized').slick(config);}
else{$(id).not('.slick-initialized').slick({slidesToShow:1,slidesToScroll:1,arrows:true,dots:true,fade:true,autoplay:true,autoplaySpeed:10000,});}}};});